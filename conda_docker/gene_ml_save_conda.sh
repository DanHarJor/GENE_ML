conda env export --name gene_ml > gene_ml_env.yml #gene_ml_env_stable.yml
# line below won't work in bash sctipt so run manually for pip compatibility
# conda init && conda activate gene_ml && conda list -e > requirements.txt
